---
title: Next.js Frontend
description: Next.js 16 frontend application for Nexus Hotel booking platform
---

The frontend is a Next.js 16 application located in `apps/web/` that provides the user interface for the Nexus Hotel booking platform.

## Technology Stack

<CardGroup cols={2}>
  <Card title="Next.js 16" icon="react">
    App Router with React Server Components
  </Card>
  <Card title="React 19" icon="atom">
    Latest React features and concurrent rendering
  </Card>
  <Card title="TypeScript" icon="code">
    Type-safe development
  </Card>
  <Card title="Turbopack" icon="bolt">
    Fast development server
  </Card>
</CardGroup>

## Package Configuration

From `apps/web/package.json`:

```json apps/web/package.json
{
  "name": "web",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack --port 3001",
    "build": "next build",
    "start": "next start",
    "lint": "next lint --max-warnings 0",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@repo/ui": "workspace:*",
    "@supabase/supabase-js": "^2.94.0",
    "next": "^16.0.10",
    "react": "^19.2.1",
    "react-dom": "^19.2.1"
  }
}
```

## Application Structure

```
apps/web/
├── app/
│   ├── layout.tsx           # Root layout with fonts
│   ├── page.tsx             # Home page
│   ├── globals.css          # Global styles
│   ├── habitaciones/        # Rooms page
│   ├── restaurantes/        # Restaurant page
│   └── experiences/         # Experiences page
├── components/
│   ├── Navbar/             # Navigation component
│   ├── Hero/               # Hero section
│   ├── Services/           # Services grid
│   ├── Features/           # Features section
│   ├── Footer/             # Footer component
│   ├── Button/             # Button component
│   ├── Card/               # Card component
│   └── AuthModal/          # Authentication modal
├── utils/
│   └── supabase/
│       └── client.ts       # Supabase client setup
├── next.config.js          # Next.js configuration
├── tsconfig.json           # TypeScript configuration
└── package.json            # Package dependencies
```

## Root Layout

The root layout (`apps/web/app/layout.tsx`) sets up custom fonts and metadata:

```tsx apps/web/app/layout.tsx
import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
});

const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable}`}>
        {children}
      </body>
    </html>
  );
}
```

## Home Page

The main page (`apps/web/app/page.tsx`) composes multiple components:

```tsx apps/web/app/page.tsx
import Navbar from '../components/Navbar';
import Hero from '../components/Hero';
import Services from '../components/Services';
import Features from '../components/Features';
import Footer from '../components/Footer';

export default function Home() {
  return (
    <div>
      <Navbar />
      <main>
        <Hero />
        <Services />
        <Features />
      </main>
      <Footer />
    </div>
  );
}
```

## Key Features

### Pages and Routes

<Tabs>
  <Tab title="Home">
    **Route:** `/`
    
    Main landing page featuring:
    - Navigation bar
    - Hero section
    - Services showcase (Rooms, Dining, Events)
    - Features highlights
    - Footer
  </Tab>
  
  <Tab title="Rooms">
    **Route:** `/habitaciones`
    
    Hotel rooms browsing and booking interface.
  </Tab>
  
  <Tab title="Restaurant">
    **Route:** `/restaurantes`
    
    Restaurant table reservation system.
  </Tab>
  
  <Tab title="Experiences">
    **Route:** `/experiences`
    
    Special experiences and event packages.
  </Tab>
</Tabs>

### Development Server

The app runs with Turbopack on port 3001:

```bash
next dev --turbopack --port 3001
```

### Styling Approach

- **CSS Modules**: Component-scoped styles
- **Global CSS**: Shared styles and utilities
- **Custom Fonts**: Geist Sans and Geist Mono
- **Glass Morphism**: Modern UI effects

## Next.js Configuration

Minimal configuration in `apps/web/next.config.js`:

```javascript apps/web/next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
```

## TypeScript Setup

The app uses shared TypeScript configurations from the monorepo:

```json apps/web/tsconfig.json
{
  "extends": "@repo/typescript-config/nextjs.json",
  "compilerOptions": {
    "plugins": [
      {
        "name": "next"
      }
    ]
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts"
  ],
  "exclude": [
    "node_modules"
  ]
}
```

## Development Workflow

<Steps>
  <Step title="Install Dependencies">
    ```bash
    pnpm install
    ```
  </Step>
  
  <Step title="Start Development Server">
    ```bash
    pnpm dev
    ```
    
    The web app will be available at `http://localhost:3001`
  </Step>
  
  <Step title="Type Checking">
    ```bash
    pnpm check-types
    ```
  </Step>
  
  <Step title="Linting">
    ```bash
    pnpm lint
    ```
  </Step>
  
  <Step title="Build for Production">
    ```bash
    pnpm build
    ```
  </Step>
</Steps>

## Key Dependencies

| Package | Version | Purpose |
|---------|---------|----------|
| next | ^16.0.10 | Next.js framework |
| react | ^19.2.1 | React library |
| react-dom | ^19.2.1 | React DOM rendering |
| @supabase/supabase-js | ^2.94.0 | Supabase client |
| @repo/ui | workspace:* | Shared UI components |
| @repo/eslint-config | workspace:* | ESLint configuration |
| @repo/typescript-config | workspace:* | TypeScript configuration |

## Related Documentation

<CardGroup cols={3}>
  <Card title="Components" icon="puzzle-piece" href="/app/components">
    React component library
  </Card>
  <Card title="Database" icon="database" href="/app/database">
    Supabase integration
  </Card>
  <Card title="Shared Packages" icon="box" href="/app/shared-packages">
    Monorepo shared packages
  </Card>
</CardGroup>